---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { projects } from '../i18n/projects';

const base = import.meta.env.BASE_URL;
const lang = 'pl';
const featured = projects;
---

<Layout title="Projektowanie konstrukcji prefabrykowanych | Konstrukcje WM" lang="pl">
  <Header slot="header" lang="pl" />

  <!-- HERO -->
  <section class="hero">
    <div class="hero-bg"></div>
    <div class="hero-overlay"></div>
    <div class="hero-grid-pattern"></div>
    <div class="container hero-content">
      <div class="hero-badge">
        <span class="hero-badge-dot"></span>
        Pracownia Projektowa · Gdańsk
      </div>
      <h1>
        Twoja inwestycja<br/>mogłaby kosztować<br/>
        <span class="gold">30% mniej.</span>
      </h1>
      <p class="hero-subtitle">
        Sprawdzimy to za darmo. Specjalizujemy się w projektowaniu konstrukcji prefabrykowanych.
        Optymalizujemy budynki, skracamy czas budowy i obniżamy koszty — w Polsce, Szwecji, Danii i Niemczech.
      </p>
      <div class="hero-buttons">
        <a href={`${base}kontakt`} class="btn-primary">Bezpłatna analiza projektu →</a>
        <a href={`${base}realizacje`} class="btn-secondary">Nasze realizacje</a>
      </div>
      <div class="hero-stats">
        <div class="hero-stat">
          <div class="hero-stat-value">4</div>
          <div class="hero-stat-label">Kraje<br/>działalności</div>
        </div>
        <div class="hero-stat">
          <div class="hero-stat-value">50+</div>
          <div class="hero-stat-label">Zrealizowanych<br/>projektów</div>
        </div>
        <div class="hero-stat">
          <div class="hero-stat-value">BIM</div>
          <div class="hero-stat-label">Technologia<br/>projektowania</div>
        </div>
        <div class="hero-stat">
          <div class="hero-stat-value">3</div>
          <div class="hero-stat-label">Języki<br/>dokumentacji</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ABOUT / PROBLEM → SOLUTION -->
  <section class="section about">
    <div class="container">
      <div class="about-grid">
        <div class="about-image-wrapper observe">
          <div class="about-image-accent"></div>
          <img src={`${base}images/projects/ikea/budowa.jpg`} alt="Budowa — Konstrukcje WM" class="about-image" />
          <div class="about-image-accent-2"></div>
        </div>
        <div class="about-text observe">
          <div class="section-label">Znamy Twój problem</div>
          <h2 class="section-title">Monolit jest drogi<br/>i wolny?</h2>
          <p>Konstrukcja monolityczna wydłuża czas budowy i podnosi koszty. Nie wiesz, czy prefabrykacja zadziała w Twoim projekcie. Potrzebujesz dokumentacji w obcym języku.</p>
          <p><strong style="color: var(--gold)">Nasze rozwiązanie:</strong> Zamieniamy monolit na prefabrykację — oszczędzasz czas i pieniądze. Bezpłatna analiza — sprawdzimy i powiemy wprost. Dokumentacja w 3 językach: PL, EN, DE.</p>
          <div class="about-features">
            <div class="about-feature">
              <div class="about-feature-icon">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><path d="M22 4 12 14.01l-3-3"/></svg>
              </div>
              Normy Eurocode
            </div>
            <div class="about-feature">
              <div class="about-feature-icon">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
              </div>
              PL · SE · DK · DE
            </div>
            <div class="about-feature">
              <div class="about-feature-icon">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
              </div>
              Technologia BIM
            </div>
          </div>
          <div class="about-langs">
            <span class="lang-badge">🇵🇱 Polski</span>
            <span class="lang-badge">🇬🇧 English</span>
            <span class="lang-badge">🇩🇪 Deutsch</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- SERVICES -->
  <section class="section services">
    <div class="container">
      <div class="section-header-center">
        <div class="section-label">Oferta</div>
        <h2 class="section-title">Kompleksowe usługi<br/>projektowe</h2>
        <p class="section-desc">Od konstrukcji prefabrykowanych po optymalizację — zapewniamy pełne wsparcie projektowe na każdym etapie inwestycji.</p>
      </div>

      <div class="grid-2">
        <a href={`${base}uslugi/prefabrykacja`} class="service-card observe">
          <div class="service-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/></svg>
          </div>
          <span class="service-number">01</span>
          <h3>Konstrukcje Prefabrykowane</h3>
          <p>Projekty elementów konstrukcji: słupów, belek, podwalin, schodów, ścian oraz dźwigarów strunobetonowych. Pliki .pxml i .uni pod automatyczne linie produkcyjne.</p>
        </a>
        <a href={`${base}uslugi/konstrukcje-zelbetowe`} class="service-card observe">
          <div class="service-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M3 21h18"/><path d="M5 21V7l7-4 7 4v14"/><path d="M9 21v-4h6v4"/></svg>
          </div>
          <span class="service-number">02</span>
          <h3>Konstrukcje Żelbetowe</h3>
          <p>Projekty techniczne i wykonawcze dla wszystkich rodzajów konstrukcji żelbetowych. Zamiana konstrukcji monolitycznej na prefabrykowaną z zachowaniem pełnej efektywności.</p>
        </a>
        <a href={`${base}uslugi/sprezone`} class="service-card observe">
          <div class="service-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
          </div>
          <span class="service-number">03</span>
          <h3>Konstrukcje Sprężone</h3>
          <p>Projekty technologiczne i programy sprężania dla konstrukcji kablobetonowych i strunobetonowych. Projekty warsztatowe elementów strunobetonowych.</p>
        </a>
        <a href={`${base}uslugi/optymalizacja`} class="service-card observe">
          <div class="service-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 20V10"/><path d="M18 20V4"/><path d="M6 20v-4"/></svg>
          </div>
          <span class="service-number">04</span>
          <h3>Optymalizacja Konstrukcji</h3>
          <p>Zamieniamy konstrukcję monolityczną na prefabrykowaną. Oszczędzasz czas i koszty budowy przy zachowaniu pełnej efektywności konstrukcji.</p>
        </a>
      </div>
    </div>
  </section>

  <!-- USP -->
  <section class="section usp">
    <div class="container">
      <div class="section-label">Dlaczego my</div>
      <h2 class="section-title">Jedyne biuro projektowe w Polsce<br/>z takim doświadczeniem</h2>
      <div class="usp-grid">
        <div class="usp-item observe">
          <div class="usp-icon">🌍</div>
          <h4>4 kraje</h4>
          <p>Projekty w Polsce, Szwecji, Danii i Niemczech. Znamy specyfikę rynków i normy EC każdego kraju.</p>
        </div>
        <div class="usp-item observe">
          <div class="usp-icon">⚙️</div>
          <h4>Pliki .pxml i .uni</h4>
          <p>Generujemy pliki gotowe pod nowoczesne automatyczne linie produkcyjne prefabrykatów.</p>
        </div>
        <div class="usp-item observe">
          <div class="usp-icon">📊</div>
          <h4>BIM native</h4>
          <p>Modele 3D z pełną informacją na każdym etapie inwestycji. Płynna współpraca międzybranżowa.</p>
        </div>
        <div class="usp-item observe">
          <div class="usp-icon">📝</div>
          <h4>3 języki</h4>
          <p>Dokumentacja w języku polskim, angielskim i niemieckim — zero barier w projektach międzynarodowych.</p>
        </div>
        <div class="usp-item observe">
          <div class="usp-icon">🔄</div>
          <h4>Monolit → Prefab</h4>
          <p>Zamieniamy konstrukcję monolityczną na prefabrykowaną — tak samo efektywną, ale szybszą i tańszą.</p>
        </div>
        <div class="usp-item observe">
          <div class="usp-icon">🏭</div>
          <h4>Kompletna dokumentacja</h4>
          <p>Od projektu technicznego po warsztatowy — pełen pakiet dla inwestorów, architektów i fabryk.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- PROJECTS -->
  <section class="section projects">
    <div class="container">
      <div class="section-label">Realizacje</div>
      <h2 class="section-title">Nasze projekty</h2>
      <p class="section-desc" style="margin-bottom: var(--space-3xl)">Realizujemy wymagające projekty na terenie Polski, Szwecji, Danii oraz Niemiec.</p>

      <div class="grid-2">
        {featured.map(project => (
          <a href={`${base}realizacje/${project.slug}`} class="project-card observe">
            <div class="project-image-wrap">
              <img src={`${base}images/projects/${project.images[0]}`} alt={project.data.pl.name} loading="lazy" />
              <div class="project-image-overlay"></div>
              <span class="project-location">{project.flag} {project.country}</span>
            </div>
            <div class="project-info">
              <h3>{project.data.pl.name}</h3>
              <p>{project.data.pl.challenge}</p>
              <div class="project-tags">
                <span class="project-tag">{project.data.pl.type}</span>
                {project.data.pl.stats[0] && <span class="project-tag">{project.data.pl.stats[0].value}</span>}
              </div>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="section cta-section">
    <div class="container">
      <div class="section-label">Kontakt</div>
      <h2 class="section-title">Porozmawiajmy<br/>o Twoim projekcie</h2>
      <p class="section-desc" style="margin-bottom: var(--space-xl)">
        Zamów bezpłatną analizę — sprawdzimy, ile możesz zaoszczędzić dzięki prefabrykacji. Bez zobowiązań.
      </p>
      <div class="cta-buttons">
        <a href={`${base}kontakt`} class="btn-primary">Bezpłatna analiza projektu →</a>
        <a href="tel:+48791392370" class="btn-secondary">+48 791 392 370</a>
      </div>
    </div>
  </section>

  <Footer slot="footer" lang="pl" />
</Layout>

<style>
  /* HERO */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background: url('/konstrukcje-wm-website/images/projects/ikea/budowa.jpg') center/cover no-repeat;
    filter: brightness(0.25);
    transform: scale(1.1);
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(13, 17, 23, 0.85) 0%, rgba(13, 17, 23, 0.5) 50%, rgba(13, 17, 23, 0.7) 100%);
  }

  .hero-grid-pattern {
    position: absolute;
    inset: 0;
    background-image: linear-gradient(rgba(201, 169, 110, 0.03) 1px, transparent 1px),
                       linear-gradient(90deg, rgba(201, 169, 110, 0.03) 1px, transparent 1px);
    background-size: 60px 60px;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    padding-top: 120px;
    padding-bottom: 80px;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 16px;
    background: rgba(201, 169, 110, 0.1);
    border: 1px solid rgba(201, 169, 110, 0.2);
    border-radius: 100px;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 32px;
    animation: fadeInUp 1s ease 0.2s both;
  }

  .hero-badge-dot {
    width: 6px;
    height: 6px;
    background: var(--gold);
    border-radius: 50%;
  }

  .hero h1 {
    font-family: var(--font-heading);
    font-size: clamp(3rem, 7vw, 5.5rem);
    font-weight: 700;
    line-height: 1.05;
    margin-bottom: 24px;
    animation: fadeInUp 1s ease 0.4s both;
  }

  .hero h1 .gold {
    color: var(--gold);
  }

  .hero-subtitle {
    font-size: 1.15rem;
    color: var(--text-secondary);
    max-width: 560px;
    line-height: 1.7;
    margin-bottom: 40px;
    animation: fadeInUp 1s ease 0.6s both;
  }

  .hero-buttons {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    animation: fadeInUp 1s ease 0.8s both;
  }

  .hero-stats {
    display: flex;
    gap: 48px;
    margin-top: 64px;
    padding-top: 40px;
    border-top: 1px solid rgba(201, 169, 110, 0.15);
    animation: fadeInUp 1s ease 1s both;
  }

  .hero-stat-value {
    font-family: var(--font-heading);
    font-size: 2.2rem;
    font-weight: 700;
    color: var(--gold);
  }

  .hero-stat-label {
    font-size: 0.8rem;
    color: var(--text-muted);
    letter-spacing: 1px;
    text-transform: uppercase;
    margin-top: 4px;
    line-height: 1.4;
  }

  /* ABOUT / Problem-Solution */
  .about {
    background: var(--dark);
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3xl);
    align-items: center;
  }

  .about-image-wrapper {
    position: relative;
  }

  .about-image {
    width: 100%;
    height: 500px;
    object-fit: cover;
    border-radius: 12px;
    filter: brightness(0.9);
  }

  .about-image-accent {
    position: absolute;
    top: -16px;
    left: -16px;
    width: 120px;
    height: 120px;
    border-left: 3px solid var(--gold);
    border-top: 3px solid var(--gold);
    border-radius: 12px 0 0 0;
  }

  .about-image-accent-2 {
    position: absolute;
    bottom: -16px;
    right: -16px;
    width: 120px;
    height: 120px;
    border-right: 3px solid var(--gold);
    border-bottom: 3px solid var(--gold);
    border-radius: 0 0 12px 0;
  }

  .about-text p {
    color: var(--text-secondary);
    font-size: 1rem;
    line-height: 1.8;
    margin-bottom: 16px;
  }

  .about-features {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    margin: 24px 0;
  }

  .about-feature {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 0.9rem;
    color: var(--text-secondary);
  }

  .about-feature-icon {
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(201, 169, 110, 0.1);
    border-radius: 6px;
    color: var(--gold);
    flex-shrink: 0;
  }

  .about-langs {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin-top: 8px;
  }

  .lang-badge {
    padding: 6px 16px;
    background: rgba(201, 169, 110, 0.08);
    border: 1px solid rgba(201, 169, 110, 0.15);
    border-radius: 100px;
    font-size: 0.8rem;
    color: var(--gold);
    font-weight: 500;
  }

  /* SERVICES */
  .services {
    background: var(--dark-card);
  }

  .service-card {
    display: block;
    background: var(--dark);
    border: 1px solid var(--dark-border);
    border-radius: 12px;
    padding: 36px 28px;
    transition: 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    position: relative;
    overflow: hidden;
    text-decoration: none;
  }

  .service-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: var(--gradient-gold);
    transform: scaleX(0);
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    transform-origin: left;
  }

  .service-card:hover {
    border-color: rgba(201, 169, 110, 0.2);
    transform: translateY(-4px);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  }

  .service-card:hover::before {
    transform: scaleX(1);
  }

  .service-icon {
    width: 52px;
    height: 52px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(201, 169, 110, 0.1);
    border-radius: 10px;
    margin-bottom: 20px;
    color: var(--gold);
  }

  .service-number {
    position: absolute;
    top: 20px;
    right: 24px;
    font-family: var(--font-heading);
    font-size: 3rem;
    font-weight: 700;
    color: rgba(201, 169, 110, 0.06);
    line-height: 1;
  }

  .service-card h3 {
    font-family: var(--font-heading);
    font-size: 1.2rem;
    font-weight: 600;
    margin-bottom: 12px;
    color: var(--text-primary);
  }

  .service-card p {
    font-size: 0.9rem;
    color: var(--text-secondary);
    line-height: 1.7;
  }

  /* USP */
  .usp {
    background: var(--dark);
  }

  .usp-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
    margin-top: var(--space-3xl);
  }

  .usp-item {
    padding: 24px;
    border: 1px solid var(--dark-border);
    border-radius: 12px;
    transition: 0.3s;
  }

  .usp-item:hover {
    border-color: rgba(201, 169, 110, 0.2);
  }

  .usp-icon {
    font-size: 1.5rem;
    margin-bottom: 12px;
  }

  .usp-item h4 {
    font-family: var(--font-heading);
    font-size: 1.05rem;
    margin-bottom: 8px;
    color: var(--text-primary);
  }

  .usp-item p {
    font-size: 0.85rem;
    color: var(--text-secondary);
    line-height: 1.7;
  }

  /* PROJECTS */
  .projects {
    background: var(--dark-card);
  }

  .project-card {
    display: block;
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid var(--dark-border);
    background: var(--dark);
    transition: 0.4s;
    text-decoration: none;
  }

  .project-card:hover {
    border-color: rgba(201, 169, 110, 0.2);
    transform: translateY(-4px);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  }

  .project-image-wrap {
    position: relative;
    height: 280px;
    overflow: hidden;
  }

  .project-image-wrap img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s;
  }

  .project-card:hover .project-image-wrap img {
    transform: scale(1.05);
  }

  .project-image-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(13, 17, 23, 0.6) 0%, transparent 60%);
  }

  .project-location {
    position: absolute;
    top: 16px;
    right: 16px;
    padding: 4px 12px;
    background: rgba(13, 17, 23, 0.7);
    backdrop-filter: blur(10px);
    border-radius: 100px;
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: var(--gold);
    border: 1px solid rgba(201, 169, 110, 0.2);
  }

  .project-info {
    padding: 24px;
  }

  .project-info h3 {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 8px;
  }

  .project-info p {
    font-size: 0.85rem;
    color: var(--text-secondary);
    line-height: 1.7;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
    margin-bottom: 16px;
  }

  .project-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .project-tag {
    padding: 4px 12px;
    border: 1px solid var(--dark-border);
    border-radius: 100px;
    font-size: 0.7rem;
    color: var(--text-muted);
    letter-spacing: 0.5px;
  }

  /* CTA */
  .cta-section {
    background: var(--dark);
  }

  .cta-buttons {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-stats {
      flex-wrap: wrap;
      gap: 24px;
    }

    .hero-stat {
      flex: 0 0 calc(50% - 12px);
    }

    .about-grid {
      grid-template-columns: 1fr;
    }

    .about-image {
      height: 300px;
    }

    .usp-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
