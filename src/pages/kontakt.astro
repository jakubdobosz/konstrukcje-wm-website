---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const base = import.meta.env.BASE_URL;
---

<Layout title="Kontakt — Bezpłatna analiza projektu | Konstrukcje WM" lang="pl">
  <Header slot="header" lang="pl" />

  <section class="page-hero">
    <div class="container">
      <div class="section-label">Kontakt</div>
      <h1 class="section-title">Porozmawiajmy<br/>o Twoim projekcie</h1>
      <p class="section-desc">Zamów bezpłatną analizę — sprawdzimy, ile możesz zaoszczędzić dzięki prefabrykacji. Bez zobowiązań.</p>
    </div>
  </section>

  <section class="section">
    <div class="container contact-grid">
      <div class="contact-form-wrap observe">
        <h2>Bezpłatna analiza projektu</h2>
        <p class="form-desc">Wypełnij formularz, a skontaktujemy się w ciągu 24h.</p>
        <form class="contact-form" action="https://formspree.io/f/biuro@konstrukcje-wm.pl" method="POST">
          <div class="form-group">
            <label for="name">Imię i nazwisko</label>
            <input type="text" id="name" name="name" required />
          </div>
          <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required />
          </div>
          <div class="form-group">
            <label for="phone">Telefon</label>
            <input type="tel" id="phone" name="phone" />
          </div>
          <div class="form-group">
            <label for="message">Opis projektu</label>
            <textarea id="message" name="message" rows="5" placeholder="Typ budynku, lokalizacja, szacowana powierzchnia, pytania..."></textarea>
          </div>
          <button type="submit" class="btn-primary" style="width:100%;justify-content:center;">Wyślij zapytanie →</button>
        </form>
      </div>

      <div class="contact-info observe">
        <div class="info-card">
          <h3>Dane kontaktowe</h3>
          <div class="info-item">
            <span class="info-icon">📞</span>
            <div>
              <span class="info-label">Telefon</span>
              <a href="tel:+48791392370">+48 791 392 370</a>
            </div>
          </div>
          <div class="info-item">
            <span class="info-icon">✉️</span>
            <div>
              <span class="info-label">Email</span>
              <a href="mailto:biuro@konstrukcje-wm.pl">biuro@konstrukcje-wm.pl</a>
            </div>
          </div>
          <div class="info-item">
            <span class="info-icon">📍</span>
            <div>
              <span class="info-label">Adres</span>
              <span>ul. Sarnia 6, 80-336 Gdańsk</span>
            </div>
          </div>
          <div class="info-item">
            <span class="info-icon">👤</span>
            <div>
              <span class="info-label">Osoba kontaktowa</span>
              <span>Mariusz Melloch</span>
            </div>
          </div>
        </div>

        <div class="info-card">
          <h3>Dane firmy</h3>
          <p>Pracownia Projektowa<br/>Konstrukcje WM</p>
          <p class="muted">NIP: 5842627831<br/>REGON: 381566723</p>
        </div>

        <div class="info-card">
          <h3>Języki dokumentacji</h3>
          <div class="lang-badges">
            <span class="lang-badge">🇵🇱 Polski</span>
            <span class="lang-badge">🇬🇧 English</span>
            <span class="lang-badge">🇩🇪 Deutsch</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <Footer slot="footer" lang="pl" />
</Layout>

<style>
  .page-hero{padding:160px 0 40px;background:var(--dark)}
  .contact-grid{display:grid;grid-template-columns:1.2fr 1fr;gap:var(--space-3xl);align-items:start}
  .contact-form-wrap h2{font-family:var(--font-heading);font-size:1.5rem;margin-bottom:8px}
  .form-desc{color:var(--text-secondary);margin-bottom:var(--space-xl);font-size:.95rem}
  .contact-form{display:flex;flex-direction:column;gap:var(--space-md)}
  .form-group{display:flex;flex-direction:column;gap:6px}
  .form-group label{font-size:.8rem;font-weight:600;letter-spacing:1px;text-transform:uppercase;color:var(--text-muted)}
  .form-group input,.form-group textarea{background:var(--dark-card);border:1px solid var(--dark-border);border-radius:8px;padding:12px 16px;color:var(--text-primary);font-family:var(--font-body);font-size:.95rem;transition:border-color .2s}
  .form-group input:focus,.form-group textarea:focus{outline:none;border-color:var(--gold)}
  .form-group textarea{resize:vertical}
  .contact-info{display:flex;flex-direction:column;gap:var(--space-lg)}
  .info-card{background:var(--dark-card);border:1px solid var(--dark-border);border-radius:12px;padding:var(--space-xl)}
  .info-card h3{font-family:var(--font-heading);font-size:1.1rem;margin-bottom:var(--space-md);color:var(--gold)}
  .info-item{display:flex;align-items:flex-start;gap:12px;margin-bottom:16px}
  .info-item:last-child{margin-bottom:0}
  .info-icon{font-size:1.2rem}
  .info-label{display:block;font-size:.7rem;font-weight:600;letter-spacing:1px;text-transform:uppercase;color:var(--text-muted);margin-bottom:2px}
  .info-item a,.info-item span{font-size:.95rem;color:var(--text-secondary)}
  .info-item a:hover{color:var(--gold)}
  .info-card p{color:var(--text-secondary);line-height:1.7;font-size:.95rem}
  .info-card .muted{color:var(--text-muted);font-size:.85rem;margin-top:8px}
  .lang-badges{display:flex;gap:8px;flex-wrap:wrap}
  .lang-badge{padding:6px 16px;background:rgba(201,169,110,.08);border:1px solid rgba(201,169,110,.15);border-radius:100px;font-size:.8rem;color:var(--gold);font-weight:500}
  @media(max-width:768px){.contact-grid{grid-template-columns:1fr}}
</style>
